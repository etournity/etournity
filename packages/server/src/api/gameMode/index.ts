import { GameModeType } from './gameMode.type'
import { GameModeQueries } from './gameMode.queries'
import { GameModeMutations } from './gameMode.mutations'

export const GameModeSchema = [GameModeType, GameModeQueries, GameModeMutations]
